docker-ubuntu-runit-sshd
========================

Docker image based on stackbrew/ubuntu base images.

Added to the base image are:
* runit as a container init system.
* openssh sshd managed by runit

